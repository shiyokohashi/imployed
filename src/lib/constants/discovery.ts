/** Careers shown per browse/discover page (max before pagination). */
export const CAREERS_PER_PAGE = 50;

/** @deprecated Use CAREERS_PER_PAGE */
export const MIN_CAREERS_PER_PAGE = CAREERS_PER_PAGE;

/** Top N matches get expanded detail cards; the rest use compact rows. */
export const DISCOVERY_DETAIL_CUTOFF = 20;
