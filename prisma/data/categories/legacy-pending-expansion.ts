import type { CareerCategoryDefinition } from "../career-taxonomy.types";

/** Legacy placeholder — all categories now use batch files in prisma/data/categories/. */
export const LEGACY_CATEGORIES: CareerCategoryDefinition[] = [];
